cmake_minimum_required(VERSION 3.0)
include("compile/var.cmake")
include("compile/import.cmake")
include("compile/links.cmake")

project(${compo_project_name})
set(CMAKE_CXX_STANDARD 11)
file(GLOB_RECURSE file_dep src/*.cpp)

add_library(${compo_lib_name} SHARED ${file_dep})
target_include_directories("${compo_lib_name}" PUBLIC ${_include_directories})

set_target_properties("${compo_lib_name}"
                      PROPERTIES
                      ARCHIVE_OUTPUT_DIRECTORY lib
                      LIBRARY_OUTPUT_DIRECTORY lib)

